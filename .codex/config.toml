# Draft project-level Codex config for gpu-mon.
# If your local Codex setup does not auto-load repo-local config,
# merge this entry into ~/.codex/config.toml.
# Copy the example block below and replace the path with your local clone path.
# Keep this file as documentation only; do not commit machine-specific trust entries.

#[projects."/path/to/gpu-mon"]
#trust_level = "trusted"
