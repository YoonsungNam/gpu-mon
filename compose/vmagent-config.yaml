global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "mock-dcgm"
    static_configs:
      - targets:
          - "mock-dcgm-exporter:9400"
        labels:
          env: homelab
          cluster: macbook
          workload_type: training
